<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Krafs1"
        tools:targetApi="31">
        <activity
            android:name=".ForumDetail"
            android:exported="false" />
        <activity
            android:name=".ForumPage"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MerchantPage">

            <!-- Include any intent filters or other activity properties as needed -->
        </activity>
        <activity android:name=".ArticlePage">

            <!-- Include any intent filters or other activity properties as needed -->
        </activity>
        <activity android:name=".LoginPage">

            <!-- Include any intent filters or other activity properties as needed -->
        </activity>
        <activity android:name=".RegisterPage">

            <!-- Include any intent filters or other activity properties as needed -->
        </activity>

    </application>


</manifest>